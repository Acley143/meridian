// GENERATED -- DO NOT EDIT.
// Source: contracts/openapi/service-api.yaml
// Regenerate via `make gen` (tools/codegen/generate.py), which shells out to
// the pinned openapi-typescript in tools/codegen/openapi-ts-codegen. A
// hand-edit here is silently overwritten on the next regeneration and will
// be flagged by the CI drift check before that (contracts/README.md).
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/v1/portfolios": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a portfolio.
         * @description Creates a portfolio row, appends a `portfolio_created` audit entry, and publishes a `portfolio.state` record with an empty `positions` array, all in one transaction (ADR-0024).
         *
         *     **Not idempotency-key based, unlike `POST /trades` (ADR-0024).** `portfolio_id` is client-supplied and is itself the resource's identity; a portfolio is fully specified by its own fields, so a retry is detected by comparing the persisted row, not a raw request fingerprint. A retry with the same `portfolio_id` and the same `name`/`base_currency`/`owner` returns `200` with the stored portfolio (no second audit entry, no second `portfolio.state` message); the same `portfolio_id` with any of those three fields different returns `409`.
         */
        post: operations["createPortfolio"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch a portfolio's metadata. */
        get: operations["getPortfolio"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}/positions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch the current positions for a portfolio. */
        get: operations["listPositions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}/risk": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch the latest risk snapshot for a portfolio, under the latest pricer_version. */
        get: operations["getLatestRisk"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}/risk/history": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Bounded range query over a portfolio's risk snapshot history.
         * @description Returns snapshots with `as_of` in `[from, to]`, most recent first, capped at `limit`. This is a REST alternative to `/api/v1/.../risk/stream` for a client that wants a fixed window of history rather than a live feed -- e.g. to render a chart on page load before subscribing to the stream for updates.
         */
        get: operations["getRiskHistory"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}/risk/stream": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Server-Sent Events stream of RiskSnapshot updates for a portfolio.
         * @description Resume semantics per ADR-0012. Each event's `id:` field is `{portfolio_id}:{as_of_micros}:{pricer_version}` -- the ADR-0007 identity tuple. On reconnect the browser's EventSource automatically sends `Last-Event-ID` with the id of the last event it received; the service parses it and replays persisted snapshots for this portfolio with a later `as_of` than the one in the header. Replay is bounded at 500 snapshots or 15 minutes, whichever is smaller -- beyond that bound the service emits a `resync` event instead of replaying, and the client must refetch full state via `GET /api/v1/portfolios/{id}/positions` and `GET /api/v1/portfolios/{id}/risk`.
         */
        get: operations["streamRisk"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/trades": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Book a trade.
         * @description Synchronous REST booking endpoint (Q1 default; see services/core-service/PLAN.md open questions for whether this stays REST or moves to an inbound Kafka topic). Updates the affected portfolio's positions and produces a PortfolioState message (ADR-0003).
         *
         *     **Idempotent (ADR-0023).** `Idempotency-Key` is required. A retry with the same key and the same request body replays the original `201` (or `400`) response verbatim, including the original `trade_id` -- it does not book a second trade. The same key with a *different* request body is rejected with `409` and never replayed. A key is scoped to this endpoint only; reusing it against a different endpoint is not a collision.
         */
        post: operations["bookTrade"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/instruments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create an instrument.
         * @description Creates an instrument row, appends an `instrument_created` audit entry (with a null `portfolio_id` -- ADR-0025), and publishes a `reference.instruments` record, all in one transaction (ADR-0025).
         *
         *     **Not idempotency-key based, for the same reason as `POST /portfolios` (ADR-0024, cited by ADR-0025).** `instrument_id` is client-supplied and is itself the resource's identity; an instrument is fully specified by its own fields, so a retry is detected by comparing the persisted row, not a raw request fingerprint. A retry with the same `instrument_id` and every other field matching returns `200` with the stored instrument (no second audit entry, no second `reference.instruments` message); the same `instrument_id` with any field different returns `409`.
         *
         *     **Conditional fields.** `option_type`, `strike`, and `expiry` must all be present when `instrument_type` is `VANILLA_EUROPEAN_OPTION` or `VANILLA_AMERICAN_OPTION`, and all absent otherwise -- present-and-non-null on a non-option is rejected with `400`, not silently ignored.
         */
        post: operations["createInstrument"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/portfolios/{portfolioId}/audit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch audit log entries for a portfolio.
         * @description Contract defined in Q1; the hash-chained audit log itself (ADR-0008) is a Q2 deliverable in services/core-service/PLAN.md. This endpoint may 501 until then.
         */
        get: operations["getAudit"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description See docs/domain-model.md#portfolio. */
        Portfolio: {
            portfolio_id: string;
            name: string;
            /** @description ISO 4217 currency code. */
            base_currency: string;
            owner: string;
        };
        /** @description Body of POST /api/v1/portfolios (ADR-0024). Same fields as Portfolio -- creation takes the full resource up front, since portfolio_id is client-supplied and there is no server-assigned identity to return separately. */
        PortfolioRequest: {
            portfolio_id: string;
            name: string;
            /** @description ISO 4217 currency code. Validated against ^[A-Z]{3}$ only -- not checked against the ISO 4217 table itself. */
            base_currency: string;
            owner: string;
        };
        /** @description See docs/domain-model.md#instrument and contracts/avro/reference-instruments.avsc -- field-for-field the same shape published to reference.instruments. option_type, strike, and expiry are always present in the response, null unless instrument_type is VANILLA_EUROPEAN_OPTION or VANILLA_AMERICAN_OPTION. */
        Instrument: {
            instrument_id: string;
            underlying_id: string;
            /** @enum {string} */
            instrument_type: "EQUITY" | "VANILLA_EUROPEAN_OPTION" | "VANILLA_AMERICAN_OPTION";
            /** @enum {string|null} */
            option_type: "CALL" | "PUT" | null;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. */
            strike: string | null;
            /** Format: date-time */
            expiry: string | null;
            /** @description ISO 4217 currency code. */
            currency: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. */
            contract_size: string;
        };
        /** @description Body of POST /api/v1/instruments (ADR-0025). Same fields as Instrument -- creation takes the full resource up front, since instrument_id is client-supplied and there is no server-assigned identity to return separately. option_type, strike, and expiry are required together when instrument_type is VANILLA_EUROPEAN_OPTION or VANILLA_AMERICAN_OPTION, and must all be absent otherwise. */
        InstrumentRequest: {
            instrument_id: string;
            underlying_id: string;
            /** @enum {string} */
            instrument_type: "EQUITY" | "VANILLA_EUROPEAN_OPTION" | "VANILLA_AMERICAN_OPTION";
            /** @enum {string|null} */
            option_type?: "CALL" | "PUT" | null;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Required iff instrument_type is an option type. */
            strike?: string | null;
            /**
             * Format: date-time
             * @description Required iff instrument_type is an option type.
             */
            expiry?: string | null;
            /** @description ISO 4217 currency code. Validated against ^[A-Z]{3}$ only -- not checked against the ISO 4217 table itself. */
            currency: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Must be strictly positive. */
            contract_size: string;
        };
        /** @description See docs/domain-model.md#position. */
        Position: {
            portfolio_id: string;
            instrument_id: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Signed -- positive is long. */
            quantity: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. */
            average_cost: string;
            /**
             * Format: date-time
             * @description UTC, microsecond precision.
             */
            as_of_event_time: string;
        };
        /** @description See docs/domain-model.md#risksnapshot, ADR-0007, and contracts/avro/risk-snapshot.avsc (this is the same shape as the wire schema). */
        RiskSnapshot: {
            portfolio_id: string;
            /**
             * Format: date-time
             * @description Part of the ADR-0007 identity tuple. Distinct from ingest_time.
             */
            as_of: string;
            pricer_version: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Portfolio-level mark-to-market value. */
            price: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Cash delta (ADR-0017) -- currency change per 1% relative move in spot, summed across positions. Not the per-unit float64 delta docs/conventions.md defines for quant_core; raw per-unit Greeks aren't summable across a portfolio's different underlyings. */
            cash_delta: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Cash gamma (ADR-0017) -- change in cash_delta per 1% move in spot, summed across positions. */
            cash_gamma: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Cash vega (ADR-0017) -- currency per 1.00 absolute change in volatility, not per 1% (docs/conventions.md), summed across positions. */
            cash_vega: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Cash theta (ADR-0017) -- currency per calendar year, not per day (docs/conventions.md), summed across positions. */
            cash_theta: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Cash rho (ADR-0017) -- currency per 1.00 absolute change in rate (docs/conventions.md), summed across positions. */
            cash_rho: string;
            /** Format: double */
            var_95: number;
            /** @description Propagated from the Tick stream that produced this snapshot (ADR-0011), for end-to-end lineage. */
            scenario_id: string;
            /**
             * Format: date-time
             * @description UTC, microsecond precision. The earliest event_time among the prices actually used to price this snapshot's positions -- see docs/domain-model.md#risksnapshot. Equal to as_of when every input was priced off the triggering tick itself; earlier than as_of means at least one position's price is resting on a tick that hasn't updated since, i.e. this snapshot is live but stale-but-not-wrong. The epoch value (1970-01-01T00:00:00Z) is a sentinel meaning "unknown, produced by a pricer that predates this field" -- not a real staleness value -- per the Avro schema's BACKWARD-compatible default.
             */
            oldest_input_event_time: string;
            /** Format: date-time */
            ingest_time: string;
        };
        /** @description Body of POST /api/v1/trades. See docs/domain-model.md#trade -- trade_id and ingest_time are server-assigned, not client-supplied. */
        TradeRequest: {
            portfolio_id: string;
            instrument_id: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Signed -- positive is a buy, negative is a sell. */
            quantity: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. Execution price per unit, not adjusted for fees. */
            price: string;
            /**
             * Format: date-time
             * @description UTC instant the trade executed upstream.
             */
            event_time: string;
        };
        /** @description See docs/domain-model.md#trade. */
        Trade: {
            trade_id: string;
            portfolio_id: string;
            instrument_id: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. */
            quantity: string;
            /** @description Decimal (precision 38, scale 8) encoded as a string, per ADR-0004/ADR-0013. */
            price: string;
            /** Format: date-time */
            event_time: string;
            /** Format: date-time */
            ingest_time: string;
        };
        /** @description See docs/domain-model.md#auditentry and ADR-0008. */
        AuditEntry: {
            entry_id: string;
            entry_type: string;
            /** @description Canonical JSON serialization of the audited event. */
            payload: string;
            /** @description Hex-encoded SHA-256 of the previous row's canonical form. Empty string for the first row only. */
            prev_hash: string;
            /** @description Hex-encoded SHA-256 of this row's own canonical form. */
            entry_hash: string;
            /** Format: date-time */
            event_time: string;
            /** Format: date-time */
            ingest_time: string;
        };
    };
    responses: never;
    parameters: {
        PortfolioId: string;
        /** @description Client-supplied opaque token identifying one logical write attempt (ADR-0023). Must not be empty. Natural-key dedup on the request body is deliberately not used -- two genuine trades can share every other field at the same instant, so only a client-chosen key tells a retry apart from a second submission. */
        IdempotencyKey: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    createPortfolio: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PortfolioRequest"];
            };
        };
        responses: {
            /** @description `portfolio_id` already existed with `name`, `base_currency`, and `owner` all matching this request -- the stored portfolio, unchanged. Not re-created; no new audit entry or `portfolio.state` message. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Portfolio"];
                };
            };
            /** @description Portfolio created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Portfolio"];
                };
            };
            /** @description A required field was missing or blank, or `base_currency` did not match `^[A-Z]{3}$`. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `portfolio_id` already existed with a different `name`, `base_currency`, or `owner`. Not applied. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getPortfolio: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Portfolio"];
                };
            };
            /** @description No portfolio with that id. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    listPositions: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Position"][];
                };
            };
        };
    };
    getLatestRisk: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RiskSnapshot"];
                };
            };
            /** @description No snapshot exists yet for that portfolio. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getRiskHistory: {
        parameters: {
            query: {
                /** @description Inclusive lower bound on `as_of`, RFC 3339. */
                from: string;
                /** @description Inclusive upper bound on `as_of`, RFC 3339. */
                to: string;
                /** @description Maximum snapshots returned. Same bound as SSE replay (ADR-0012). */
                limit?: number;
            };
            header?: never;
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RiskSnapshot"][];
                };
            };
            /** @description `from` is after `to`, or the range exceeds what the service is willing to scan in one request. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    streamRisk: {
        parameters: {
            query?: never;
            header?: {
                /** @description Sent automatically by EventSource on reconnect. Format `{portfolio_id}:{as_of_micros}:{pricer_version}`, per ADR-0012. */
                "Last-Event-ID"?: string;
            };
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description text/event-stream of two event types. The default (unnamed) event carries a RiskSnapshot JSON payload per `data:` line, with an `id:` line per ADR-0012. An `event: resync` carries no RiskSnapshot payload and signals the client to refetch full state via REST because the requested replay exceeded the bound in ADR-0012. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/event-stream": components["schemas"]["RiskSnapshot"];
                };
            };
        };
    };
    bookTrade: {
        parameters: {
            query?: never;
            header: {
                /** @description Client-supplied opaque token identifying one logical write attempt (ADR-0023). Must not be empty. Natural-key dedup on the request body is deliberately not used -- two genuine trades can share every other field at the same instant, so only a client-chosen key tells a retry apart from a second submission. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TradeRequest"];
            };
        };
        responses: {
            /** @description Trade booked. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Trade"];
                };
            };
            /** @description Malformed trade (e.g. non-positive contract_size on the referenced instrument, unknown portfolio_id/instrument_id), or a missing/empty `Idempotency-Key` header. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `Idempotency-Key` was already used with a request body that does not match this one (ADR-0023). The original trade was not re-booked and this request was not applied. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    createInstrument: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InstrumentRequest"];
            };
        };
        responses: {
            /** @description `instrument_id` already existed with every other field matching this request -- the stored instrument, unchanged. Not re-created; no new audit entry or `reference.instruments` message. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Instrument"];
                };
            };
            /** @description Instrument created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Instrument"];
                };
            };
            /** @description A required field was missing, `instrument_type`/`option_type` did not parse into the generated Avro enum, a conditional-field rule was violated in either direction, `currency` did not match `^[A-Z]{3}$`, `contract_size` was not strictly positive, or `strike`/`expiry` was unparseable. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `instrument_id` already existed with at least one field different. Not applied. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getAudit: {
        parameters: {
            query?: {
                limit?: number;
            };
            header?: never;
            path: {
                portfolioId: components["parameters"]["PortfolioId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK, most recent first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEntry"][];
                };
            };
            /** @description Audit log not yet implemented (Q1). */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
}
